mod using_the_match_method;
mod match_literal_strings;
mod regex_or_operator;
mod ignore_case_while_matching;

fn main() {
    /* Regular Expressions (RegEx) -
       Define a search pattern that can be used to 
       to search for things in a string. */
}
